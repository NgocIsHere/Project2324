
con.Open();
            string deleteQuery = $"DELETE FROM NHANVIEN WHERE ID = @RowID";

            using (SqlCommand command = new SqlCommand(deleteQuery, con))
            {
                // Add a parameter for the condition
                command.Parameters.AddWithValue("@RowID", "NV345678");

                // Execute the DELETE command
                int rowsAffected = command.ExecuteNonQuery();

                if (rowsAffected > 0)
                {
                    Console.WriteLine($"Deleted {rowsAffected} row(s) successfully.");
                }
                else
                {
                    Console.WriteLine("No rows were deleted. Check your condition.");
                }
            }
            con.Close();